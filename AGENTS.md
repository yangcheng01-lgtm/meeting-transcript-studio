# 多听工作台 Agent 执行约定

## 当前冲刺

在 2026-09-15 至 2026-09-16 完成可录制、可提交的 MVP，并准备 3–5 分钟演示视频。工作以真实闭环为准：导入音频/视频/公开链接或多文件，完成 Speaker 分离、人工命名、带时间戳逐字稿、按 Skill 生成结构化交付物。

## 北极星结果

1. 导入本地音频、视频、多文件和公开 YouTube 链接。
2. 本地 pyannote 分离 Speaker，支持预设人数、代表发言和试听片段。
3. 公司 qwen3-asr 按 Speaker 时间块转写；具备失败重试、断点续跑和业务术语提示。
4. 使用者确认姓名/角色并校对文本，导出 Markdown、TXT、SRT。
5. 从多个总结 Skill 中选择输出：会议纪要、业务访谈洞察、培训/讲解整理、通用音视频报告。
6. 长逐字稿自动分段提取再汇总，保留事实、数字、人物归属和来源时间戳。
7. 模型设置支持公司网关 URL、个人 API Key 和模型选择；Key 只驻留当前进程。
8. 中文、英文、两人真实高光至少各有一个稳定样本；演示项目可重复运行。
9. 提交物包含源码、测试、验收清单、技术架构/能力边界/迭代方向和演示视频脚本。
10. 完成测试、敏感信息扫描、Git commit 并 push 到 GitHub。

## 模型路由与额度

- **默认低成本**：`external/glm-5.3-flash`，¥0.170/M，倍率 ×0.130。用于常规总结、Skill 测试、文档和高频迭代。
- **长上下文**：`external/qwen3.7-plus`，¥0.700/M，倍率 ×0.550。用于较长逐字稿的分段提取或合并。
- **质量增强**：`external/glm-5.3`，¥1.570/M，倍率 ×1.230。用于最终报告复核和关键样本。
- **复杂工程推理**：`external/gpt-5.6-luna`，¥1.520/M，倍率 ×1.150。仅在代码或架构难题确实需要时使用。
- 优先外部模型；高倍率 `external/gpt-5.6-sol`、`external/gpt-5.6-terra` 不作为常规选择。
- 可调用子 Agent；每个子任务选择满足质量要求的最低倍率模型，写集必须互不冲突。
- 最多使用当前剩余额度的 80%，保留至少 20% 作为最终回归、视频补录和故障重试余量。

## 执行顺序

1. 先修 P0 闭环和稳定性，再做 UI 润色与扩展能力。
2. 每次模型调用先用短样本验证，再扩展到 60 秒和长会议。
3. 长任务必须可恢复；已有成功片段应复用，避免重复消耗。
4. 每个新增 Skill 都用同一份带人名、带时间戳逐字稿做回归。
5. 视频使用预先跑通的固定项目，录制时隐藏 API Key，避免现场首次调用不稳定链路。

## 完成判定

只有下列条件全部满足才称为完成：

- 端到端中文样本成功：导入 → Speaker → ASR → 人名 → 逐字稿 → 总结报告。
- 英文样本和多文件样本至少完成到带人名逐字稿。
- 四个总结 Skill 可发现、可选择、可下载结果。
- 术语词表同时进入 ASR 提示和总结提示。
- ASR 重跑能命中缓存，单片失败可自动重试。
- 长逐字稿不会以单次超长 prompt 直接提交。
- 自动化测试通过，浏览器关键流程通过，API Key/HF Token 未写入仓库。
- GitHub 与本地 main 同步；验收清单和演示脚本可直接使用。

## 交互与安全

- 仅在缺少凭证、不可逆选择或外部状态阻塞时向用户提问；其余情况直接推进。
- 原始音视频、逐字稿、声纹信息和业务材料按敏感数据处理，保存在 D 盘本地数据目录，不提交 GitHub。
- API Key、HF Token、Cookie 和模型凭证不得写入项目文件、日志、导出物或 Git 历史。
- 对声纹实名识别保持人工确认和合规边界；MVP 只做 Speaker 聚类与人工命名。
