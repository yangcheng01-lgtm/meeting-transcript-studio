# 内网语音逐字稿平台 — 部署方案 v1

> 更新日期：2026-09-08  
> 状态：基于单机 MVP 验证结果，待申请内网服务器资源后进入 POC 部署。

## 1. 已确认的业务约束

| 项目 | 已确认决策 | 对架构的影响 |
|---|---|---|
| 使用频率 | 偶尔使用；高峰允许排队 | 采用异步任务队列，而非高并发实时服务 |
| 当前设备 | 笔记本；可申请服务器 | 笔记本继续承担 MVP/开发；正式处理迁至内网服务器 |
| 服务器资源 | 可以申请 | 推荐部署为内网中央工作站，浏览器只做操作界面 |
| 数据保留 | 永久保存 | 原始音视频、最终逐字稿、speaker 标记和导出物须进入持久化存储与备份 |

## 2. 推荐目标架构

```text
同事浏览器
  │  上传音视频 / 编辑 speaker / 下载逐字稿
  ▼
内网 Web 服务（HTTPS + SSO / 权限）
  ├── 项目数据库：项目、任务状态、speaker 名称、角色、编辑记录
  ├── 文件存储：原始音视频、最终逐字稿、SRT、审计附件
  ├── 任务队列：等待中 → 转码 → diarization → ASR → 对齐 → 可编辑
  └── 后台 Worker（CPU / GPU）
       ├── ffmpeg：转码为 16 kHz 单声道 WAV
       ├── pyannote：离线说话人分离
       ├── faster-whisper：本地带时间戳 ASR
       └── 对齐服务：ASR segment × Speaker 时间线
```

### 为什么不采用“每个浏览器下载模型并运行”

当前 MVP 依赖 Python、PyTorch、pyannote、音频解码与聚类流程。即使模型权重约几十 MB，完整运行环境约 1.7 GB，且长音频推理会持续占用 CPU/GPU。浏览器端部署会带来：

- 每位同事重复下载、版本难统一；
- 同事电脑性能差异大，长录音可能卡死浏览器；
- 页面关闭、电脑休眠会中断任务；
- 不便集中存储、排队、审计与权限控制；
- 模型分发与 gated 访问条款管理复杂。

因此采用“模型常驻内网服务器、用户仅通过浏览器操作”的方式。

## 3. HF 模型与 Token 策略

```text
首次部署 / 模型升级：管理员临时提供 HF read token
     ↓
下载并验证 pyannote 模型缓存
     ↓
日常服务：HF_HUB_OFFLINE=1
     ↓
普通使用者：不需要 HF token
```

日常推理只从服务器本地缓存加载模型；不将音频上传到 Hugging Face。

- 管理员 token 不写入代码、数据库、前端、项目文件或日志；
- 建议仅在模型初始化/升级维护窗口临时注入；
- 服务运行阶段开启离线模式；
- 访问公司 ASR（若使用路线 A）是另一条链路，使用公司 ASR 凭证，不使用 HF token。

## 4. 服务器资源申请建议

### POC / 偶尔使用（可排队）

适用于：每天少量任务、单个长录音依次处理。

```text
OS：Windows Server 或 Linux（推荐 Linux）
CPU：8 vCPU
内存：32 GB RAM
系统盘：100 GB SSD
工作盘：至少 500 GB SSD
GPU：可选；没有 GPU 也可跑，但处理长录音更慢
网络：仅公司内网可访问
```

### 推荐生产基线（优先申请）

适用于：多人偶尔使用、峰值排队、需要更快处理长录音。

```text
CPU：16 vCPU
内存：64 GB RAM
GPU：NVIDIA GPU，显存至少 16 GB；建议 24 GB 档位
本地 NVMe 工作盘：1 TB
持久化存储：NAS / 内部对象存储，初始 2 TB，可扩容
```

GPU主要用于加快 ASR；pyannote 与 ASR 都可在 CPU 运行，但对 1 小时音频，CPU 任务可能持续数十分钟甚至更久。队列模式可以保证服务稳定，但 GPU 会显著改善等待体验。

## 5. 永久保存策略

### 必须永久保存

```text
原始音频 / 视频
最终确认的带署名逐字稿（Markdown / TXT / SRT）
speaker 名称、角色及人工修订记录
任务元数据：模型版本、处理时间、来源、操作人、导出记录
```

### 可再生成、不建议长期占用主存储的中间产物

```text
16 kHz WAV
ASR 临时切片
调试日志
模型推理中间缓存
```

建议默认保留原始音视频和最终成果；中间 WAV 可在任务成功后归档到低成本存储或在确认后清理，因为它可以由原始文件重新生成。

### 容量估算（基于当前样本）

当前样本约 58 分钟：

```text
原始 M4A：约 56 MB
派生 16 kHz 单声道 WAV：约 110 MB
```

因此每 1,000 小时录音，粗略估算：

| 保存策略 | 主数据规模（不含备份） |
|---|---:|
| 原始压缩音频 + 最终逐字稿 | 约 55–70 GB |
| 原始音频 + WAV 中间件 + 最终成果 | 约 160–190 GB |

至少建议三层保护：主存储、异机备份、定期恢复演练。若使用 3 副本原则，应在容量申请中按 3 倍估算。

## 6. 权限、隐私与审计

音频和声纹/说话人信息属于高敏感业务数据。内网版最低要求：

- 公司 SSO 或至少内部账号 + 强密码；
- 项目按部门/创建人做访问隔离；
- 上传、下载、导出、删除行为写审计日志；
- 仅允许受控上传和受控共享目录，不能让远程用户填服务器任意绝对路径；
- HTTPS 或可信内网反向代理；
- 数据库与存储备份权限独立；
- 跨录音自动识别具体身份（真正声纹注册）上线前，另行完成授权、告知和数据治理评审。

## 7. 分阶段实施

### Phase 0 — 已完成：单机 MVP

```text
离线 pyannote diarization
+ 本地 faster-whisper timestamp ASR
+ Speaker 人工命名/角色标记
+ 自动对齐
+ Markdown / TXT / SRT 导出
```

### Phase 1 — 内网 POC

```text
单台内网服务器
+ 单 Worker
+ 单队列
+ 受控上传
+ 永久存储目录
+ 管理员离线模型缓存
+ 5–10 位试用者
```

验收：高峰任务排队、任务完成后可编辑和导出、权限不会越界、原文件与成果可恢复。

### Phase 2 — 受控推广

```text
SSO / 部门权限
+ PostgreSQL / Redis
+ 后台 Worker 独立进程
+ NAS / 对象存储
+ 操作审计
+ 自动清理中间产物
```

### Phase 3 — 质量与身份增强

```text
优先接入支持 timestamps 的 qwen3-asr（若验证通过）
+ 更大本地 ASR 模型 / GPU 推理
+ 术语词表与关键词校正
+ 经审批的注册声样和跨录音身份匹配
```

## 8. 下一步建议

1. 用本地 MVP 再处理 1–2 份真实中文会议录音，评估文本校对量；
2. 以“推荐生产基线”申请内网服务器；
3. 明确最终要使用的存储位置（NAS、对象存储或服务器附加盘）；
4. 明确 SSO/账号接入方式；
5. 获得服务器后，将现有工作台从单机文件项目升级为 Web 服务 + 队列 + 持久化存储。

## 9. 存储口径与人工确认辅助（2026-09-08 补充）

### 保留原始文件的好处

- 作为可追溯的权威源：文本、speaker 标签或会议纪要有争议时可回听核对；
- ASR / diarization 模型升级后可重新处理，无需重新向录音人索取文件；
- 可修复术语、专有名词或时间线对齐错误；
- 可为后续经审批的声纹注册或质量评估提供原始依据。

### 当前样本的实测体积

以“新录音 8”（约 58 分 47 秒）为例：

| 产物 | 实测大小 | 建议 |
|---|---:|---|
| 原始 M4A | 55.99 MB | 永久保存 |
| 派生 16 kHz 单声道 WAV | 107.63 MB | 可再生；完成验收后转低成本归档或清理 |
| diarization JSON | 约 0.14 MB | 永久保存 |
| ASR segments JSON / Markdown / SRT | KB 级到低 MB 级 | 永久保存 |

派生 WAV 比原始压缩 M4A 增加约 51.6 MB（约 1.92 倍）。因此永久保存“原始文件 + 最终文本/元数据”几乎不增加额外存储；真正占容量的是永久保存 WAV 与大量可再生临时切片。

### 人工确认 Speaker 的辅助能力

工作台的 Speaker 卡片现已显示：

- 匿名 Speaker 的累计发言时长；
- 已有对齐文本时的前两段代表发言；
- 未对齐时的提示；
- 最长代表发言片段的起止时间；
- “试听代表片段”按钮。

代表片段由本机 ffmpeg 导出为短 WAV，仅在本地项目的 `speaker_previews` 目录缓存。该缓存可由原始音频和 diarization 结果重新生成，不属于必须永久保留的成果。

## 10. 公司 qwen3-asr 的时间戳能力结论

2026-09-08 实测：qwen3-asr 服务可返回转写 text，但当前网关未返回 OpenAI `verbose_json` 的标准 `segments`。因此，不能将“原始长音频 qwen 转写”和“Speaker 时间线”直接按 qwen timestamps 对齐。

生产推荐采用 Speaker-aware qwen 路由：先完成本地 pyannote，再按 Speaker 发言块调用 qwen。此路由的时间边界由 pyannote 提供、文字由 qwen 提供；适用于偶尔使用且允许任务排队的业务约束。

不建议为追求并行而直接使用 300 秒 qwen 分片做自动署名：其时间范围太粗，可能跨越多个说话人。

### 120 秒 qwen Speaker-aware 性能实测

在 CPU 本地 pyannote 已完成后，120 秒中文对话合并为 36 个 qwen 请求块；顺序调用公司 qwen3-asr 耗时约 2 分 40 秒，并成功生成 36 段带 Speaker 逐字稿。该速度适合“偶尔使用、允许高峰排队”的业务约束；服务器部署时建议用队列控制并发。
